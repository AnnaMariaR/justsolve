version: '3.8'

services:
  laravel:
    image: composer:2
    container_name: justsolve_laravel
    working_dir: /app
    volumes:
      - ../backend:/app
    ports:
      - "8000:8000"
    command: php artisan serve --host=0.0.0.0 --port=8000
